# Docker
# Build a Docker image
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

trigger:
- master

pool:
  name: Default

resources:
- repo: self

variables:
  tag: '$(Build.BuildId)'

stages:
- stage: Crear
  displayName: Docker image
  jobs:
  - job: Crear
    displayName: Crear
#    pool:
#      vmImage: ubuntu-latest
#    pool:
#      name: Default
    steps:
#    - task: Docker@2
#      displayName: Build an image
#      inputs:
#        command: 'build'
#        Dockerfile: '**/Dockerfile'
#        tags: '$(tag)'


    - task: Bash@3
      inputs:
        targetType: 'inline'
        script: |
          # Write your commands here
          docker build -t libreclinica:2023 .
          docker rm -f libreclinica
          docker run -d --name libreclinica -p 8183:8080 libreclinica:2023
          docker ps --filter name=libreclinica

          echo 'Hello world'


